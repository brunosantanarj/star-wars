{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "lint": "lerna run lint",
    "server": "lerna run start:watch --parallel"
  }
}
