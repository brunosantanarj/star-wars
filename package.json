{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "postinstall": "lerna bootstrap",
    "lint": "lerna run lint",
    "watch": "lerna run --parallel start:watch"
  }
}
